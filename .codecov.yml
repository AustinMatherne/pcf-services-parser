coverage:
  range: "70...90"
  status:
    project:
      default:
        target: 80
        threshold: 2
    patch:
      default:
        target: 80
        threshold: 2

comment:
  layout: "reach, diff, flags, files"
